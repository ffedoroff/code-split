// Baseline/current is now rendered as two separate clean diagrams (one graphviz layout
// per snapshot), selected via the Baseline/Current nav buttons — there is no chip-based
// filtering of a single merged layout anymore. This module is intentionally empty;
// it is kept as a file because the report inlines its assets by name.
